// Crear un programa que diga si la fecha ingresada corresponde o no a navidad.

let dia = Number(prompt("Ingrese el dia: "));
let mes = Number(prompt("Ingrese el mes: "));
let año = Number(prompt("Ingrese el año: "));

if (dia == 25 && mes == 12) {
  document.write(`La fecha ${dia}/${mes}/${año} corresponde a navidad.`);
} else {
  document.write(`La fecha ${dia}/${mes}/${año} corresponde a navidad.`);
}